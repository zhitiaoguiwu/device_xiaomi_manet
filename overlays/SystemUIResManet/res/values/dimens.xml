<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2024 The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources xmlns:xliff="urn:oasis:names:tc:xliff:document:1.2">
    <!-- the padding on the top of the statusbar (usually 0) -->
    <dimen name="status_bar_padding_top">14.0px</dimen>

    <!-- the padding on the start of the statusbar -->
    <dimen name="status_bar_padding_start">4dp</dimen>

    <!-- the padding on the end of the statusbar -->
    <dimen name="status_bar_padding_end">4dp</dimen>

    <!-- Height of the status bar header bar when on Keyguard -->
    <dimen name="status_bar_header_height_keyguard">13dp</dimen>

    <!-- Adjust the icon padding in status bar for rounded corners. -->
    <dimen name="rounded_corner_content_padding">81.0px</dimen>

    <!-- Match the stock K70 Pro bottom status-bar inset. -->
    <dimen name="status_bar_padding_bottom">18.0px</dimen>

    <!-- Keep the keyguard indication above the in-display fingerprint sensor. -->
    <dimen name="keyguard_indication_margin_bottom">500px</dimen>

    <!-- Location on the screen of the center of the physical power button. -->
    <dimen name="physical_power_button_center_screen_location_y">1460px</dimen>

    <!-- Microns/ums (1000 um = 1mm) per pixel for the given device. If unspecified, UI that
         relies on this value will not be sized correctly. -->
    <item format="float" name="pixel_pitch" type="dimen">48.2</item>
</resources>
