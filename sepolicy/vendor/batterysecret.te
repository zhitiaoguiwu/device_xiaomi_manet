# Charger authentication inspects the USB Type-C topology before enabling
# Xiaomi fast-charging modes. The common policy already grants search.
allow batterysecret vendor_sysfs_usb_c:dir { read getattr };
