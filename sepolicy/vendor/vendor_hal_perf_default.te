# Perf HAL identifies the power optimization service before applying boosts.
allow vendor_hal_perf_default vendor_hal_poweroptservice_qti:file r_file_perms;
allow vendor_hal_perf_default vendor_thermal_data_file:dir r_dir_perms;
